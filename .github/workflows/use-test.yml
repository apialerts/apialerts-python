name: Test

on:
  workflow_call:

jobs:
  use-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python & Dependencies
        uses: ./.github/workflows/use-python.yml

      - name: Run tests
        run: python -m unittest discover tests